APPNAME = require '../'
should = require 'should'
require 'mocha'

describe 'APPNAME', ->
  describe 'FUNCTIONNAME()', ->
    it 'should TASKNAME', (done) ->
      should.exist true
      true.should.equal.true
      done()